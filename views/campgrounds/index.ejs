<%=layout('layout/boiler') %>
        <% for (let camp of campgrounds) { %>
            <div class="container-md mt-4">
                <div class="card mb-3">
                    <div class="row g-0">
                        <div class="col-md-4">
                            <img src=<%= camp.image %> alt="" class="img-fluid rounded-start mt-1">
                        </div>
                        <div class="col-md-8">
                            <div class="card-body">
                                <h5 class="card-title"><%= camp.title %></h5>
                                <p class="card-text" style="text-align: justify;"><%= camp.description %></p>
                                <p class="card-text"><small class="text-secondary"><%= camp.location %></small></p>
                                <a href="/campgrounds/<%=camp._id%>" class="btn bg-info-subtle text-body-emphasis mt-4">View <%=camp.title%></a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        <% } %>
